import React from 'react';
import PropTypes from 'prop-types';
import classnames from 'classnames';
import './logo-space.less';

const LogoSpace = ({className}) => (
  <div className={classnames('logo-space', className)}>
    Edookit space logotype
  </div>
)


export default LogoSpace;
